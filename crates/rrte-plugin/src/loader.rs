//! Plugin loader implementation (stub)

/// Represents a dynamically loaded plugin.
#[derive(Debug, Default)]
pub struct PluginLoader;

impl PluginLoader {
    /// Create a new loader
    pub fn new() -> Self { Self }
}
